The user has proposed an idea for the AT-DJ project: $ARGUMENTS

**Step 1 — Clarify:**
Restate your understanding of the idea clearly and concisely, covering:
- What the idea is (in plain terms)
- Where it would fit in the AT-DJ system (which module, which work package, which user-facing feature)
- Any dependencies, trade-offs, or implementation implications you see
- Whether it is a new feature, an enhancement to an existing one, or an architectural change

Then ask: *"Is this understanding correct? Anything to add or change before I record it?"*

**Step 2 — Record (only after the user confirms):**
Append the idea to `doc/ideas.md` in this exact format:

```
- [ ] **<Short idea title>**
  - *Summary:* <One-sentence description>
  - *Clarification:* <The confirmed understanding from Step 1 — what it is, where it fits, key implications>
```

Do NOT record before the user confirms. Do NOT create a separate file — always append to `doc/ideas.md`.

**Ongoing rule:**
Before starting any implementation task, check `doc/ideas.md` for unchecked items that are relevant to that task. When an idea is fully implemented, mark it as checked: `- [x]`.
